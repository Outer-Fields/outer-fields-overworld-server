package io.mindspice.outerfieldsserver.combat.enums;

public enum PawnIndex {
    PAWN1(1),
    PAWN2(2),
    PAWN3(3);

    public final int idx;
    PawnIndex(int idx) {
        this.idx = idx;
    }
}
